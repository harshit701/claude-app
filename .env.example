# Requires PostgreSQL 13 or later (the Task table's id column relies on the
# built-in gen_random_uuid() function, which was added to Postgres core in 13).
DATABASE_URL=postgresql://user:password@localhost:5432/taskdb
